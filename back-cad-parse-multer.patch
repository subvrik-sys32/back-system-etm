diff --git a/src/modules/engineering/controllers/nesting-run.controller.ts b/src/modules/engineering/controllers/nesting-run.controller.ts
index 814de3a..2dc2749 100644
--- a/src/modules/engineering/controllers/nesting-run.controller.ts
+++ b/src/modules/engineering/controllers/nesting-run.controller.ts
@@ -11,6 +11,7 @@ import {
   BadRequestException,
 } from '@nestjs/common'
 import { FileInterceptor } from '@nestjs/platform-express'
+import { memoryStorage } from 'multer'
 import {
   NestingRunService,
   type NestRunRequest,
@@ -18,6 +19,11 @@ import {
 import { NestingJobService } from '../services/nesting-job.service'
 import { CadParseService } from '../services/cad-parse.service'
 
+const cadUpload = FileInterceptor('file', {
+  storage: memoryStorage(),
+  limits: { fileSize: 40 * 1024 * 1024 },
+})
+
 @Controller('engineering')
 export class NestingRunController {
   constructor(
@@ -26,7 +32,6 @@ export class NestingRunController {
     private readonly cadParse: CadParseService,
   ) {}
 
-  /** Sync nest (compat). */
   @Post('nest')
   @UsePipes(
     new ValidationPipe({
@@ -39,7 +44,6 @@ export class NestingRunController {
     return this.nesting.run(body)
   }
 
-  /** Async job. */
   @Post('nest/jobs')
   @UsePipes(
     new ValidationPipe({
@@ -62,14 +66,22 @@ export class NestingRunController {
     return this.jobs.cancel(id)
   }
 
-  /** DXF | GEO → NestingPiece[] (multi-pieza DXF). PDF se parsea en el cliente. */
+  /** DXF | GEO → NestingPiece[]. PDF se parsea en el cliente. */
   @Post('cad/parse')
-  @UseInterceptors(FileInterceptor('file'))
+  @UseInterceptors(cadUpload)
   async parseCad(
-    @UploadedFile() file?: { buffer: Buffer; originalname?: string },
+    @UploadedFile()
+    file?: {
+      buffer?: Buffer
+      originalname?: string
+      size?: number
+      mimetype?: string
+    },
   ) {
     if (!file?.buffer?.length) {
-      throw new BadRequestException('file required')
+      throw new BadRequestException(
+        'file required (multipart field "file", memory storage)',
+      )
     }
     return this.cadParse.parseUpload(
       file.originalname || 'upload.dxf',
@@ -78,12 +90,18 @@ export class NestingRunController {
   }
 
   @Post('cad/parse-dxf')
-  @UseInterceptors(FileInterceptor('file'))
+  @UseInterceptors(cadUpload)
   async parseDxf(
-    @UploadedFile() file?: { buffer: Buffer; originalname?: string },
+    @UploadedFile()
+    file?: {
+      buffer?: Buffer
+      originalname?: string
+    },
   ) {
     if (!file?.buffer?.length) {
-      throw new BadRequestException('file required')
+      throw new BadRequestException(
+        'file required (multipart field "file", memory storage)',
+      )
     }
     return this.cadParse.parseUpload(
       file.originalname || 'upload.dxf',
